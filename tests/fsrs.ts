import {
    difficulty,
    initialDifficulty,
    stability,
    initialStability,
    retrievability,
} from '../services/fsrs';


console.log('Topic One: initial rating = good');
let topicOne = {
    rating = 'good'
};

let topicOneInitialDifficulty = initialDifficulty;
console.log('Initial difficulty: ', topicOneInitialDifficulty);

